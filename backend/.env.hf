# Hugging Face Space Configuration
# Note: For security reasons, API keys should be set in Space Secrets, not here

# Use SQLite for persistence in Hugging Face Spaces
DATABASE_URL=sqlite:///./todo_app_hf.db

# Secret key for JWT tokens (change this in production)
SECRET_KEY=huggingface-space-temp-secret-key-change-in-production

# Debug mode (set to false in production)
DEBUG=true

# Model configuration
OPENROUTER_MODEL=google/gemini-2.5-flash

# For Hugging Face Spaces, these will typically be set in Space Secrets:
# OPENROUTER_API_KEY=your-api-key-here
# BETTER_AUTH_SECRET=your-auth-secret-here